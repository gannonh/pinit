{
  "name": "quick-pin",
  "displayName": "Quick Pin",
  "publisher": "Decahedra",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/decahedra/pinit"
  },
  "description": "Allows you to pin folders and files",
  "version": "0.0.2",
  "engines": {
    "vscode": "^1.82.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/main.js",
  "contributes": {
    "views": {
      "explorer": [
        {
          "id": "pins",
          "name": "Pins",
          "when": "pinit.showTreeView === true",
          "icon": "media/icon.svg",
          "contextualTitle": "Pinit pins"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "pins",
        "contents": "No pins currently held."
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "quick-pin.pinItem"
        }
      ],
      "view/item/context": [
        {
          "when": "view == pins",
          "command": "quick-pin.deletePin"
        }
      ],
      "view/title": [
        {
          "command": "quick-pin.deleteAllPins",
          "when": "view == pins",
          "group": "navigation"
        }
      ]
    },
    "commands": [
      {
        "command": "quick-pin.pinItem",
        "title": "Pin Item"
      },
      {
        "command": "quick-pin.deletePin",
        "title": "Delete Pin"
      },
      {
        "command": "quick-pin.revealAndOpen",
        "title": "Reveal and then open a selected folder"
      },
      {
        "command": "quick-pin.deleteAllPins",
        "title": "Delete all pins",
        "icon": "$(notebook-delete-cell)"
      }
    ],
    "keybindings": [
      {
        "command": "quick-pin.pinItem",
        "key": "ctrl+p ctrl+i",
        "mac": "shift+cmd+i",
        "when": "explorerViewletFocus || editorFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run esbuild-base -- --minify",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "deploy": "vsce publish",
    "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=out/main.js --external:vscode --format=cjs --platform=node",
    "esbuild": "npm run esbuild-base -- --sourcemap",
    "esbuild-watch": "npm run esbuild-base -- --sourcemap --watch"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.82.0",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "@vscode/test-electron": "^2.3.4",
    "esbuild": "^0.19.2",
    "eslint": "^8.47.0",
    "glob": "^10.3.3",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6",
    "prettier": "^2.8.8"
  }
}
